#pragma once

namespace Ivy {
    namespace Graphics {
        enum class UpdateType {
            Immutable,
            Static,
            Dynamic,
        };

        struct IVertexBuffer {

        };
    }
}